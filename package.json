{
  "name": "brew2u",
  "version": "1.0.0",
  "description": "A program that allows users to find breweries based on location.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^14.4.0",
    "gulp": "^3.9.1"
  }
}
